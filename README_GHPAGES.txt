# Quality — GitHub Pages dist
Arquivos prontos para publicar no GitHub Pages (paths relativos, runtimes com dedupe).

Publicação:
1) Copie tudo desta pasta para a raiz do repo (ou para `docs/` se usar Pages via docs/).
2) Faça commit e push.
3) Settings -> Pages: Branch `main`, folder `/` (ou `/docs`).
