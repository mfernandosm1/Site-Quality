<% layout('layout') -%>
<div class='card'><h3>🧾 Sobre (Visual)</h3><form method='post' action='/sobre/salvar'><textarea name='html' id='editor_sobre'><%- html %></textarea><div style='margin-top:10px'><button class='btn primary'>Salvar</button></div></form></div>
<script>
  if (window.CKEDITOR) {
    CKEDITOR.replace('editor_sobre', {
      extraAllowedContent: '*(*);*{*};a[!href];img[!src,alt,width,height]',
      removePlugins: 'cloudservices,easyimage',
      fullPage: true,
      allowedContent: true
    });
  }
</script>

<div class="card">
  <h3>Importar do arquivo atual</h3>
  <form method="get" action="/sobre">
    <button class="btn">Recarregar do HTML</button>
  </form>
  <p class="muted">Traz o conteúdo atual do arquivo sem precisar mexer em código.</p>
</div>
