(()=>{const form=document.getElementById('stockCountCreateForm');if(!form)return;const totalAlert=document.getElementById('totalCountAlert');const syncTotalAlert=()=>{const type=form.querySelector('input[name=type]:checked')?.value||'partial';if(totalAlert)totalAlert.classList.toggle('show',type==='total')};form.querySelectorAll('input[name=type]').forEach(r=>r.addEventListener('change',syncTotalAlert));syncTotalAlert();form.addEventListener('submit',async e=>{e.preventDefault();const error=document.getElementById('createCountError');const button=form.querySelector('button[type=submit]');error.hidden=true;button.disabled=true;const old=button.textContent;button.textContent='Iniciando...';try{const type=form.querySelector('input[name=type]:checked')?.value||'partial';const response=await fetch('/erp/estoque/contagens',{method:'POST',headers:{'Content-Type':'application/json',Accept:'application/json'},body:JSON.stringify({type,blockPdv:document.getElementById('blockPdv').checked,notes:document.getElementById('countNotes').value})});const data=await response.json();if(!response.ok||!data.success)throw new Error(data.message||'Não foi possível iniciar a contagem.');location.href=data.redirectUrl;}catch(err){error.textContent=err.message;error.hidden=false;button.disabled=false;button.textContent=old;}});})();

window.addEventListener('pageshow',()=>{const form=document.getElementById('stockCountCreateForm');const button=form?.querySelector('button[type=submit]');const error=document.getElementById('createCountError');if(button){button.disabled=false;button.textContent='Iniciar contagem';}if(error)error.hidden=true;});
