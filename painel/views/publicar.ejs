<% layout('layout') -%>
<header><div class="brand container">Painel Quality — Publicar</div></header>
<div class="container">
  <% if (flash && flash.ok) { %>
    <div class="card success mt-6">
      <h2>✅ Publicação concluída</h2>
      <p><%= flash.message %></p>
      <% if (flash.backup) { %><p><strong>Backup:</strong> <code><%= flash.backup %></code></p><% } %>
      <div class="actions mt-4">
        <a class="btn" href="/">Voltar ao painel</a>
      </div>
    </div>
  <% } else { %>
    <div class="card mt-6">
      <h2>🚀 Publicar site</h2>
      <p>Cria backup da pasta <code>Site_with_content</code>, sincroniza alterações na raiz do repositório e faz commit/push.</p>
      <form method="post" action="/publicar">
        <button class="btn primary" type="submit">Publicar agora</button>
      </form>
    </div>
  <% } %>
</div>
