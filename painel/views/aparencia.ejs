<% layout('layout') -%>
<div class="card">
  <h3>🎨 Aparência</h3>
  <p class="notice">Essas alterações geram <code>css/theme-overrides.css</code> e aplicam via <code>header.html</code>.</p>
  <form method="post" action="/aparencia/salvar" class="row">
    <div><label>Cor primária</label><input type="text" name="primary" value="<%= theme.primary || '#c90000' %>"></div>
    <div><label>Cor do texto</label><input type="text" name="text" value="<%= theme.text || '#111827' %>"></div>
    <div><label>Cor de fundo</label><input type="text" name="bg" value="<%= theme.bg || '#ffffff' %>"></div>
    <div><label>Raio (px)</label><input type="number" name="radius" value="<%= theme.radius || '10' %>"></div>
    <div style="grid-column:1/-1"><label>Fonte (CSS)</label><input type="text" name="font" value="<%= theme.font || 'Inter, system-ui, sans-serif' %>"></div>
    <div style="align-self:end"><button class="btn primary" type="submit">Salvar Aparência</button></div>
  </form>
</div>
